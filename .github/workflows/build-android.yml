name: Build Android APK

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-android:
    name: Build Debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: tashbee-app/package-lock.json

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: 'platform-tools build-tools-34 platform;android-34'

      - name: Install dependencies
        working-directory: tashbee-app
        run: npm ci

      - name: Prebuild native project
        working-directory: tashbee-app
        run: npx expo prebuild --platform android --clean

      - name: Grant gradlew permission
        working-directory: tashbee-app/android
        run: chmod +x ./gradlew

      - name: Build Debug APK
        working-directory: tashbee-app/android
        run: ./gradlew assembleDebug --no-daemon -x lint

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: tashbeeh-debug-apk
          path: tashbee-app/android/app/build/outputs/apk/debug/*.apk
          retention-days: 30
